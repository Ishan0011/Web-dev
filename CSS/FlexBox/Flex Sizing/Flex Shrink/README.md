# flex-shrink in CSS

The `flex-shrink` property controls how much a flex item shrinks relative to other flex items when space is limited.

It determines how items reduce their size to fit within the flex container.

A higher `flex-shrink` value causes an item to shrink more compared to others.
