# flex-basis in CSS

The `flex-basis` property defines the initial size of a flex item before extra space is distributed.

It sets the default main size of an item along the main axis.

Flex-basis works together with `flex-grow` and `flex-shrink` to control flex sizing behavior.
