# Selectors in CSS

CSS selectors are used to select HTML elements that you want to style.

They define which elements a set of CSS rules should apply to and are a fundamental part of writing CSS.

Common types of selectors include element selectors, class selectors, ID selectors, and combinators.
