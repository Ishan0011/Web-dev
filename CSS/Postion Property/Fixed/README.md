# Fixed Position in CSS

The `fixed` value positions an element relative to the viewport.

The element stays in the same position even when the page is scrolled.

Fixed positioning is commonly used for navigation bars, headers, and floating action buttons.
