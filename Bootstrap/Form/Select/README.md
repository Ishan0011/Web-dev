# Bootstrap `form-select`

`form-select` is a Bootstrap class used to create **styled dropdown (select) menus** in forms. It provides a **clean, responsive, and consistent design** across different devices and browsers.

### 📌 Syntax

`<select class="form-select"> </select>`  
