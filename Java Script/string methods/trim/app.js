let mes = "  Hello World  ";
console.log(mes.trim());  // removes whitespace from both ends of a string
console.log(mes.trimStart());  // removes whitespace from the start of a string
console.log(mes.trimEnd());  // removes whitespace from the end of a string     
console.log(mes); // original string remains unchanged
